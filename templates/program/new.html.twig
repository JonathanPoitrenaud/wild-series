{% extends 'base.html.twig' %}

{% block title %}Create new program{% endblock %}

{% block body %}
    <div class="container text-center">
        <div class="mt-5">
            {{ form_start(form) }}
            {{ form_widget(form) }}
            <button type="submit" class="mt-3">Add program</button>
            {{ form_end(form) }}
        </div>
        <div>
            <a href="{{ path('program_index') }}"></a>
        </div>
    </div>
{% endblock %}